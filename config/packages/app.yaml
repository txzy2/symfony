parameters:
    app.timezone: '%env(APP_TIMEZONE)%'
    app.token_secret: '%env(TOKEN_SECRET)%'

    # Параметр нужный для защиты опеределенных контроллеров
    app.protected_routes:
        - 'users_register'
        - 'edit_user'
        - 'users_get'
